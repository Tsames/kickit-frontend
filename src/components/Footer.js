//Dependencies
import { React } from 'react';

//Styling
import '../styles/footer.scss';

const Footer = ({ form, type, name, text, value, doThis }) => {

  return (
    <div className="pageFooter">
      <a src="https://github.com/Tsames/kickit-frontend">github</a>
    </div>
  )
}

export default Footer;